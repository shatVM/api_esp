<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Налаштування - ESP Receiver</title>
    <link rel="stylesheet" href="/styles.css" />
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code&display=swap" rel="stylesheet" />
    <!-- Lucide Icons CDN -->
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>
    {{> nav}}

    <div class="container">
       

        <div class="three-col" style="grid-template-columns: 1fr; max-width: 600px; margin: auto;">
            <aside class="sidebar">
                <div id="AutoControlSettings" class="content-div">
                    <h2>Автоматичне керування</h2>
                    <h3 id="currentTime" style="margin-top:0.25rem; color:#374151; font-weight:600;"></h3>
                    <input type="hidden" id="currentTimeHidden" name="currentTime" />
                    <form id="configForm">
                        <div class="form-switch sensor-item">
                            <input type="checkbox" id="enableAutoLight" name="enableAutoLight">
                            <label for="enableAutoLight">Увімкнути авто-освітлення (пін 12)</label>
                        </div>
                        <div class="form-group sensor-item">
                            <label for="autoLightStartTime">Час початку (HH:MM)</label>
                            <input type="time" id="autoLightStartTime" name="autoLightStartTime" class="form-control">
                        </div>
                        <div class="form-group sensor-item">
                            <label for="autoLightEndTime">Час закінчення (HH:MM)</label>
                            <input type="time" id="autoLightEndTime" name="autoLightEndTime" class="form-control">
                        </div>
                        <div class="form-note" style="margin-top:8px; color:#6b7280; font-size:0.9rem;">
                            Останнє збереження налаштувань (UTC+2): <span id="lastSavedTime">—</span>
                        </div>
                        <div class="form-group sensor-item">
                            <label for="lightThreshold">Поріг освітлення (lux)</label>
                            <input type="number" id="lightThreshold" name="lightThreshold" class="form-control">
                        </div>
                        <div class="form-group sensor-item">
                            <label for="uploadIntervalSeconds">Інтервал оновлення (сек)</label>
                            <input type="number" id="uploadIntervalSeconds" name="uploadIntervalSeconds" class="form-control">
                        </div>
                        <div style="margin-top:12px; display:flex; gap:8px;">
                            <button type="submit" class="btn success"><i data-lucide="save" style="margin-right:6px"></i>Зберегти</button>
                            <button type="reset" class="btn warning"><i data-lucide="refresh-ccw" style="margin-right:6px"></i>Скинути</button>
                        </div>
                    </form>
                </div>

                <div class="content-div card" style="margin-top: 2rem;">
                    <div class="card-body">
                        <h2 class="card-title">Загальні налаштування</h2>
                        <form id="generalForm">
                            <div class="form-group sensor-item">
                                <label for="deviceName">Назва пристрою</label>
                                <input type="text" class="form-control" id="deviceName" placeholder="ESP8266 Controller">
                            </div>

                            <h3 style="margin-top:1rem">WiFi (список мереж)</h3>
                            <div id="wifiList" style="margin-bottom:8px"></div>
                            <div style="display:flex; gap:8px; margin-bottom:8px;">
                                <button id="addWifiBtn" type="button" class="btn info"><i data-lucide="plus-circle" style="margin-right:6px"></i>Додати мережу</button>
                                <small style="align-self:center; color:#6b7280">Виберіть потрібні мережі та впорядкуйте</small>
                            </div>

                            <h3 style="margin-top:1rem">Адреси для надсилання (сервери)</h3>
                            <div id="sendAddressesList" style="margin-bottom:8px"></div>
                            <div style="display:flex; gap:8px; margin-bottom:8px;">
                                <button id="addAddressBtn" type="button" class="btn info"><i data-lucide="plus-circle" style="margin-right:6px"></i>Додати сервер/адресу</button>
                                <small style="align-self:center; color:#6b7280">Вкажіть URL або електронну адресу</small>
                            </div>

                            <div style="margin-top:12px; display:flex; gap:8px;">
                                <button type="submit" class="btn success"><i data-lucide="save" style="margin-right:6px"></i>Зберегти</button>
                                <button type="reset" class="btn warning"><i data-lucide="refresh-ccw" style="margin-right:6px"></i>Скинути</button>
                            </div>
                        </form>
                    </div>
                </div>
            </aside>
        </div>
    </div>

    <script src="/js/pin-control.js"></script>
    <script src="/js/settings.js"></script>
</body>
</html>
